export enum AuthRoutes {
  home = "/",
  upload = "/upload",
  my = "/my",
  videos = "/videos",
  series = "/series",
  playlists = "/playlists",
  encodes = "/encodes",
  moderation = "/moderation",
  settings = "/settings",
}

export enum NonAuthRoutes {
  login = "/login",
  unauthorized = "/unauthorized",
}
