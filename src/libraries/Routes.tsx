export enum AuthRoutes {
  home = "/",
  my = "/my",
  videos = "/videos",
  series = "/series",
  playlists = "/playlists",
  moderation = "/moderation",
  settings = "/settings",
}

export enum NonAuthRoutes {
  login = "/login",
  unauthorized = "/unauthorized",
}
